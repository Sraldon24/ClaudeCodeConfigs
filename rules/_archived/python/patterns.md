---
paths:
  - "**/*.py"
  - "**/*.pyi"
---
# Python Patterns
Extends [common/patterns.md](../common/patterns.md).
- **Protocol:** Use `typing.Protocol` for duck typing (e.g., repository interfaces).
- **DTOs:** Use `@dataclass` for requests/responses.
- **Resources:** Use context managers (`with`) and generators (lazy eval).
- **Ref:** See `python-patterns` skill.
