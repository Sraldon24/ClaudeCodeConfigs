---
description: Loads Playwright MCP on-demand.
---
# /browser
If `playwright` MCP not in project `.claude.json`, add it: `claude mcp add playwright -- npx -y @playwright/mcp`.
Use tools. When task finishes, optionally advise: `claude mcp remove playwright` to save tokens.
