---
description: Auto-searches archived coding patterns via MCP.
---
# /find
Use `search_archived_skills(query)` MCP tool to dynamically retrieve architectural patterns (e.g., langgraph, frontend, backend) as needed. Do not list files.
